# helmfile.d/admin-service-values.yaml
releases:
  # Another microservice
  - name: auth-service
    namespace: processor
    # When using OCI, the full URI must be specified in the chart field.
    chart: oci://docker.io/booora/auth-service-helm
    version: 4.0.0
#    installed: {{ if hasKey .Values "auth-service" }}{{ index .Values "auth-service" "installAuthService" | default true }}{{ else }}true{{ end }}
    installed: {{ if hasKey .Values "installAuthService" }}{{ .Values.installAuthService | default true }}{{ else }}true{{ end }}
    values:
      - ../values/auth-service-values.yaml