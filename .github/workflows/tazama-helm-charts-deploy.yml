name: Deploy All Services

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

<<<<<<< HEAD
=======
      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Install Helmfile
        run: |
          # Using a recent, valid version of Helmfile.
          HELMFILE_VERSION="v0.160.0"
          # Correctly construct the asset filename, which includes the version number (without the 'v' prefix).
          wget "https://github.com/helmfile/helmfile/releases/tag/${HELMFILE_VERSION}/helmfile_${HELMFILE_VERSION#v}_$(uname -s | tr '[:upper:]' '[:lower:]')_amd64" -O /usr/local/bin/helmfile
          chmod +x /usr/local/bin/helmfile

>>>>>>> 6cab721b78f520c427f8a508a31eaa29dd7f8220
      - name: Set up Kubectl
        uses: azure/setup-kubectl@v3
        with:
          config-file: ${{ secrets.KUBE_CONFIG_DATA }}

      - name: Login to OCI Registry (Docker Hub)
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

#      - name: Deploy all charts
#        # Using the official helmfile-action is more robust than manual installation scripts.
#        # It handles setup of helm and helmfile, and runs the specified command.
#        uses: helmfile/helmfile-action@v2.3.1
#        with:
<<<<<<< HEAD
#          helmfile-version: 'v0.160.0'
#          # The helm-diff plugin is useful for seeing changes before they are applied.
#          helm-plugins: 'https://github.com/databus23/helm-diff@v3.8.1'
#          helmfile-args: 'apply'
=======
#          helmfile-version: 'v0.150.0'
#          helm-version: 'v3.11.0'
#          helm-plugins: >
#            https://github.com/databus23/helm-diff@v3.1.3,
#            https://github.com/jkroepke/helm-secrets@v4.2.2
#          helmfile-args: apply --environment prod
#          helmfile-auto-init: 'false'
>>>>>>> 6cab721b78f520c427f8a508a31eaa29dd7f8220
